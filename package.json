{
  "name": "career-quest-2026",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Career Quest 2026 MVP: React game frontend with Express and MariaDB API.",
  "scripts": {
    "install:all": "npm.cmd install --prefix frontend && npm.cmd install --prefix backend",
    "dev:frontend": "npm.cmd run dev --prefix frontend",
    "dev:backend": "npm.cmd run dev --prefix backend",
    "build": "npm.cmd run build --prefix frontend",
    "lint": "npm.cmd run lint --prefix frontend && npm.cmd run lint --prefix backend"
  }
}
